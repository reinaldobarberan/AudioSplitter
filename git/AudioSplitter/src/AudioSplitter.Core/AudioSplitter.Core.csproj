<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\AudioSplitter.Contracts\AudioSplitter.Contracts.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!--
      El armado del mensaje de error es interno, pero necesita prueba: es lo unico
      que ve el usuario cuando la instalacion esta mal. Se abre solo a los tests.
    -->
    <InternalsVisibleTo Include="AudioSplitter.Integracion.Tests" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
